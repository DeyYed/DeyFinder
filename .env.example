# Rename this file to .env and update with your real credentials

# Google Gemini configuration
GEMINI_API_KEY=replace-with-your-gemini-api-key
GEMINI_MODEL=gemini-2.0-flash

# Optional: point the frontend at a remote/serverless API
# Example for local development with `vercel dev`
VITE_API_BASE_URL=http://localhost:3000
